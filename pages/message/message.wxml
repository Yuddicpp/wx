<view class="component">
    <image src="{{app.globalData.user_Info.avatarUrl}}" class='photo'></image>
    <view class="name">{{app.globalData.user_Info.nickName}}</view>
</view>
<view class="mine"></view>